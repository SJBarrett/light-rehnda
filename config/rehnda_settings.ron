RehndaSettings(
    scene: CornellFeatureDemo,
    output_file: Some("renders/feature_demo.jpeg"), // ppm extended file name to output to
    num_samples: 8192,
    num_threads: None,  // None let's rehnda pick optimal
    image_width: 600, // num pixels wide
    max_depth: 15,
    camera_settings: (
        aspect_ratio: (
            width: 1.0,
            height: 1.0
        ),
        aperture: 0.0,
    )
)